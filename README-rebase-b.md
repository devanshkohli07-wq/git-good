# Rebase Exercise (Branch rebase-b)

Instructions:
1. Checkout `rebase-b`: `git checkout rebase-b`
2. Run `git rebase rebase-a`
3. A conflict will appear in `rebase.txt`.
4. Resolve the conflict, then run `git rebase --continue`.
